# 贡献说明

先阅读 [新增交易所规范](docs/adding-exchanges.md) 和 [测试验收](docs/testing.md)。

1. 确认官方是否已有实现。已有就复用，不复制维护。
2. 只提交新增协议或必要差异，避免引入策略、运行服务或做市系统 DTO。
3. PR 列出 has 变化、接口来源、市场范围、特殊参数和已执行验证。
4. 提交脱敏 fixtures，禁止提交 `.env`、密钥、签名、账户资料、内部部署地址、用户附件或完整项目快照。
5. 所有写接口使用模拟服务或 sandbox 测试；真实写入不属于默认 CI。
6. 运行 lint、测试和构建。升级 CCXT 必须更新锁文件并重新验收。

扩展 ID 不覆盖官方 ID。禁止 import 时联网或自动加载全部插件。未实现能力保留 False/None，不放置假成功路径。

发现凭据泄露时，不在公开 issue 重贴凭据；先由凭据持有人撤销并清理历史。问题报告应只带脱敏证据。
